spring:
  datasource:
    driver-class-name:
    url:
    username:
    password:

logging:
  level: error
  path: logs

mybatis:
    config-location: classpath:mybatis-config.xml
    type-aliases-package: hackday.webtoon.models
